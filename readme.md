In this task, I created a Dockerfile enabling isolated execution of the FFmpeg tool.
 I built an Ubuntu-based image, 
 installed FFmpeg on it, and configured a default run command. 
 FFmpeg commands can now be executed directly within a Docker container.