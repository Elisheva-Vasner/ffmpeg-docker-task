ן
This project contains a Java class My_password_checker that validates password strength based on multiple criteria such as length,
uppercase letters, lowercase letters, digits, special characters, and more.

The PasswordChecker.java file contains the complete solution to the password checking challenge.

The Dockerfile builds the environment needed to run the solution,
ensuring consistent setup across different machines.

