This project loads an image using OpenCV, calculates the RGB color histograms, and saves the resulting histogram plot as an image.

The project runs in a Docker container based on Python 3.12. and uses OpenCV and Matplotlib.

run the container by running tha command- `docker run image-histogram`.

the output hist image will be saved in the volume folder on your local host as `histPic.png`.
